# Rising Star Maker — MVP 开发计划

## 1. MVP 定义

> 一款单机 2D 偶像养成经营游戏：玩家在有限周数内招募一名新人，安排训练与工作，参加最终试镜，并通过明确的数值反馈把新人培养成“新星”。

本计划默认由 1 名开发者使用 **Godot 4.x（开工时固定一个稳定版本）+ GDScript** 制作 Windows 桌面端原型。所有美术、音效先使用自制占位资源；数据仅保存在本地。

### 成功标准

玩家可以在 20～30 分钟内完整经历一次：

`新游戏 → 选择艺人 → 安排行程 → 属性成长/事件反馈 → 最终试镜 → 结局 → 再来一局`

满足以下条件即为 MVP：

- 从主菜单开始并无阻塞地完成 12 周流程。
- 每周的选择会影响属性、资金、压力和最终评分。
- 至少存在 3 种可理解、可重复触发的结局。
- 游戏可保存并继续当前进度。
- 首次游玩无需外部说明即可理解主要操作。
- 导出包可在目标桌面平台离线运行，完整流程无崩溃或坏档。

## 2. 核心体验与规则

### 2.1 单局流程

1. **选择艺人**：从 3 名固定候选人中选择 1 名；候选人的初始属性和特长不同。
2. **制定周计划**：每周从行动列表中选择 2 个行动。
3. **结算本周**：更新属性、资金和压力，并展示一段结果反馈。
4. **阶段试镜**：第 4、8 周进行阶段考核，给予资金奖励。
5. **最终试镜**：第 12 周计算总评分并进入对应结局。
6. **结算**：展示关键统计和重新开始按钮。

### 2.2 状态数值

| 数值 | 范围 | 用途 |
| --- | --- | --- |
| 唱功 `vocal` | 0～100 | 影响声乐类试镜表现 |
| 舞蹈 `dance` | 0～100 | 影响舞台类试镜表现 |
| 魅力 `charm` | 0～100 | 影响综合表现和工作收益 |
| 压力 `stress` | 0～100 | 过高时削弱训练与试镜表现 |
| 资金 `money` | 不低于 0 | 支付训练费用；初始值为 1,000 |
| 周数 `week` | 1～12 | 控制单局进度 |

所有属性变化在结算界面明确显示，例如 `唱功 +8`、`压力 +12`。

### 2.3 每周行动

| 行动 | 费用/收益 | 基础效果 |
| --- | ---: | --- |
| 声乐训练 | -150 | 唱功 +8，压力 +10 |
| 舞蹈训练 | -150 | 舞蹈 +8，压力 +10 |
| 表演课 | -150 | 魅力 +8，压力 +10 |
| 商业工作 | +250 | 压力 +15；魅力达到 50 时额外 +50 |
| 休息 | 0 | 压力 -25 |

规则约束：

- 每周必须选择 2 个行动后才能结算，同一行动可重复选择。
- 资金不足时不可选择付费行动，界面需说明原因。
- 属性与压力每次结算后限制在 `0～100`，资金不允许为负数。
- 压力达到 80 后，训练获得的属性降低 50%（向下取整）。
- MVP 不加入随机失败；相同输入应产生相同结果，便于理解和测试。

### 2.4 试镜与结局

阶段试镜分数：

```text
score = vocal + dance + charm - max(0, stress - 50)
```

- 第 4 周：`score >= 120` 获得 300 资金，否则获得 100。
- 第 8 周：`score >= 165` 获得 500 资金，否则获得 150。

最终试镜分数：

```text
final_score = vocal + dance + charm - stress
```

| 最终分数 | 结局 |
| ---: | --- |
| 210 及以上 | S：闪耀新星 |
| 160～209 | A：正式出道 |
| 159 及以下 | B：继续练习 |

结算页同时展示最终分数、三项属性、剩余资金和压力，使玩家能理解结局原因。

## 3. MVP 范围

### 必须实现（P0）

- 主菜单：新游戏、继续游戏、退出。
- 3 名固定候选人的选择页。
- 周计划选择、合法性校验和结算。
- 5 种行动、3 项属性、资金与压力系统。
- 第 4、8、12 周的试镜结算。
- 3 种结局及本局统计。
- 单槽本地自动存档：每周结算后保存。
- 基础设置：主音量、全屏开关。
- 可替换的占位头像、按钮、背景和音效。

### 可以延后（P1）

- 更丰富的随机事件和剧情对话。
- 多名艺人同时培养。
- 装备、技能树、好感度、经纪公司升级。
- 角色创建、程序生成候选人。
- 配音、动画演出和正式美术。
- 成就、排行榜、云存档、账号与联网功能。
- 移动端适配、付费或广告系统。

开发期间不得用 P1 功能阻塞 P0 的完整闭环。

## 4. 页面与交互

| 页面 | 必要内容 | 完成条件 |
| --- | --- | --- |
| 主菜单 | 标题、新游戏、继续、设置、退出 | 无存档时“继续”禁用 |
| 艺人选择 | 3 张候选卡、初始属性、特长、确认按钮 | 选择后进入第 1 周 |
| 周计划 | 当前周、属性、压力、资金、行动列表、已选行动 | 选满 2 项才能结算 |
| 周结算 | 每项变化、简短反馈、阶段试镜结果（如有） | 确认后进入下一周 |
| 最终结算 | 评级、分数、最终状态、重新开始、返回菜单 | 可开启新一局 |
| 设置弹窗 | 音量、全屏、关闭 | 修改即时生效 |

交互原则：

- 可点击元素必须有悬停、按下和禁用状态。
- 数值变化使用颜色与正负号表达，但不能只依赖颜色。
- 所有页面均支持鼠标操作；键盘快捷键不是 MVP 必需项。
- 确认“新游戏”覆盖旧存档前必须二次确认。

## 5. 内容清单

### 候选人

| ID | 定位 | 唱功 | 舞蹈 | 魅力 | 特长 |
| --- | --- | ---: | ---: | ---: | --- |
| `lin` | 实力歌手 | 45 | 20 | 25 | 声乐训练额外 +2 |
| `maya` | 舞台舞者 | 20 | 45 | 25 | 舞蹈训练额外 +2 |
| `rui` | 综艺新人 | 25 | 20 | 45 | 商业工作额外 +50 资金 |

### 最低资源量

- 3 张候选人头像。
- 1 张主菜单背景、1 张养成界面背景。
- 5 个行动图标。
- 1 首循环背景音乐、3 个界面音效。
- 每种行动至少 2 条结算文本。
- 每种结局 1 张占位插图和 1 段结局文本。

文本与数值放入数据文件，不硬编码在 UI 场景中。

## 6. 技术方案

### 6.1 场景结构

```text
res://
├── assets/
│   ├── audio/
│   ├── fonts/
│   └── images/
├── data/
│   ├── actions.json
│   └── candidates.json
├── scenes/
│   ├── main_menu/
│   ├── candidate_select/
│   ├── weekly_plan/
│   ├── weekly_result/
│   ├── final_result/
│   └── settings/
├── scripts/
│   ├── autoload/
│   │   ├── game_state.gd
│   │   ├── save_manager.gd
│   │   └── audio_manager.gd
│   ├── models/
│   └── ui/
├── tests/
└── project.godot
```

### 6.2 职责边界

- `GameState`：持有当前局数据，执行行动、周推进和试镜计算；不直接操作 UI。
- `SaveManager`：将版本化存档写入 `user://save.json`，负责读取、校验和损坏存档回退。
- `AudioManager`：管理背景音乐、音效和音量设置。
- 页面脚本：只负责展示状态、收集输入和调用 `GameState`。
- JSON 数据：保存候选人和行动的静态配置。

### 6.3 最小数据结构

```gdscript
var run_state := {
    "save_version": 1,
    "candidate_id": "",
    "week": 1,
    "vocal": 0,
    "dance": 0,
    "charm": 0,
    "stress": 0,
    "money": 1000,
    "selected_actions": []
}
```

存档读取必须检查字段、类型和 `save_version`。读取失败时保留损坏文件用于排查，回到“无可用存档”状态，不让游戏崩溃。

### 6.4 开发与验证命令

使用项目锁定的 Godot 可执行文件运行以下命令；若本机命令名不同，只替换 `godot`：

```bash
# 打开编辑器
godot --editor --path .

# 检查项目可无界面启动
godot --headless --path . --quit-after 1

# 运行 tests/test_runner.gd 中的纯逻辑测试
godot --headless --path . --script res://tests/test_runner.gd

# 导出 Windows 候选版本（配置导出预设后）
godot --headless --path . --export-release "Windows Desktop" build/rising-star-maker.exe
```

测试运行器必须在任一断言失败时返回非零退出码，以便本地和 CI 使用同一命令。

## 7. 开发顺序

预计工作量为 15～20 个开发日；每个阶段都应产生可运行版本。

### M0：项目骨架（1 天）

- [ ] 创建 Godot 项目和目录结构。
- [ ] 配置窗口尺寸、拉伸模式、字体和输入。
- [ ] 创建主菜单与统一主题。
- [ ] 加入基础 CI：无界面启动检查和测试命令。

**验收：** 项目可启动、切换主菜单空页面并成功导出开发包。

### M1：纯逻辑闭环（3 天）

- [ ] 定义候选人与行动 JSON。
- [ ] 实现 `GameState`、数值限制、特长、压力惩罚。
- [ ] 实现阶段试镜、最终评分与结局判定。
- [ ] 为规则编写自动化测试。

**验收：** 测试可模拟 12 周并稳定得到预期结局；无 UI 也能完成一局。

### M2：可玩垂直切片（4 天）

- [ ] 完成艺人选择、周计划、周结算页面。
- [ ] 接通第 1～12 周流程和阶段试镜。
- [ ] 完成最终结算及重新开始。
- [ ] 加入临时图像和反馈音效。

**验收：** 玩家可只用鼠标从新游戏玩到任一结局。

### M3：存档与设置（2 天）

- [ ] 每周结算后自动保存，主菜单支持继续。
- [ ] 处理缺失、旧版本和损坏存档。
- [ ] 完成音量、全屏设置及持久化。
- [ ] 为存档往返和坏档恢复编写测试。

**验收：** 退出后可恢复到最近结算周；坏档不会导致崩溃或覆盖。

### M4：内容、平衡与发布（5～10 天）

- [ ] 补齐最低资源量和所有界面状态。
- [ ] 用 3 名候选人各通关至少 3 次，记录结果并调平。
- [ ] 完成首次游玩提示和覆盖存档确认。
- [ ] 修复阻塞、崩溃、坏档和流程卡死问题。
- [ ] 导出候选版本并执行完整验收清单。

**验收：** 新玩家无需解释可完成一局；目标平台连续完成 3 局无阻塞问题。

## 8. 测试与验收

### 自动化测试重点

- 行动正确修改属性、压力和资金。
- 资金不足时拒绝付费行动。
- 压力达到阈值时训练收益减半。
- 数值始终处于合法范围。
- 三名候选人的特长生效且不影响其他行动。
- 第 4、8 周奖励和三档结局边界值正确。
- 存档写入后可等价读回，损坏存档可安全处理。

### 每个候选版本的手工检查

- [ ] 新游戏可选择任意候选人。
- [ ] 资金不足、行动未选满等禁用状态有明确提示。
- [ ] 第 4、8 周试镜只触发一次。
- [ ] 第 12 周正确进入结局，不会继续到第 13 周。
- [ ] 保存后重启，周数与所有数值一致。
- [ ] 新游戏覆盖存档前出现确认。
- [ ] 音量与全屏设置重启后仍然生效。
- [ ] 所有按钮可点击，无文字截断和缺失资源。
- [ ] 连续完成 3 局，无崩溃、卡死或无法返回菜单。

## 9. 开工清单

按以下顺序创建首批任务，完成前 6 项即可开始功能开发：

1. 初始化 Godot 4.x 项目并提交可启动的空工程。
2. 建立目录、统一主题和场景切换入口。
3. 写入 `candidates.json` 与 `actions.json`。
4. 实现不依赖 UI 的 `GameState`。
5. 添加行动结算、试镜和结局边界测试。
6. 制作主菜单与候选人选择页。
7. 制作周计划页并接入 `GameState`。
8. 制作周结算和最终结算页。
9. 实现 `SaveManager` 与存档测试。
10. 替换占位内容、试玩调平并导出 MVP。

## 10. 变更控制

新增需求只有同时满足以下条件才进入 MVP：

1. 直接支持核心循环；
2. 不增加新的联网服务或平台依赖；
3. 不影响当前里程碑的验收；
4. 有明确、可测试的完成标准。

不满足条件的需求记录到 P1，不在 MVP 开发中临时插入。
